-SELECT *
--FROM TDI_SALES

--1 CONVERT ALL CUSTOMER NAMES TO LOWERCASE AND ORDER BY LOWERCASE CUSTOMER NAME
  --SELECT LOWER([CUSTOMER NAME]) AS LOWERCASE_CUSTOMER
  --FROM TDI_SALES
  --GROUP BY [CUSTOMER NAME]
  --ORDER BY [CUSTOMER NAME] ASC

--2 CONVERT ALL CUSTOMER NAMES TO UPPERCASE DISPLAYING THE TOTAL SALES, GROUP BY UPPERCASE CUSTOMER NAME
--SELECT UPPER([CUSTOMER NAME]) AS UPPERCASE_CUSTOMER, SUM(SALES) AS TOTAL_SALES
--FROM TDI_SALES
--GROUP BY [CUSTOMER NAME], SALES
--ORDER BY 2 DESC

--3 CONCATENATE THE CUSTOMER NAME AND CITY WITH A COMMA AND SPACE BETWEEN THEM AND ORDER THE RESULTS BY THE COMBINED STRING
--SELECT [CUSTOMER ID], [CUSTOMER NAME],[CITY], CONCAT([CUSTOMER NAME], ',',' ', CITY) AS CONCAT_NAME_CITY
--FROM TDI_SALES
--GROUP BY [CUSTOMER ID],[CUSTOMER NAME], [CITY]
--ORDER BY 2

--4 EXTRACT THE FIRST THREE CHARACTERS OF THE CUSTOMER NAMES AND DISPLAY THE RESULT ORDERING BY THE SUBSTRING
--SELECT LEFT([CUSTOMER NAME], 3) AS LEFT_NAME
--FROM TDI_SALES
--GROUP BY [CUSTOMER NAME]
--ORDER BY 1

--5 GET THE FIRST FIVE CHARACTERS OF THE CUSTOMER NAMES AND CALCULATE THE TOTAL SALES OF THESE GROUPED NAMES
--SELECT LEFT([CUSTOMER NAME], 5) AS LEFT_NAME, SUM(SALES) AS TOTAL_SALES
--FROM TDI_SALES
--GROUP BY [CUSTOMER NAME], SALES
--ORDER BY 1

--6 GET THE LAST THREE CHARACTERS OF THE CUSTOMER NAME AND DISPLAY THE RESULT ORDERING BY THE LAST THREE CHARACTERS
--SELECT RIGHT([CUSTOMER NAME], 3) AS RIGHT_NAME
--FROM TDI_SALES
--GROUP BY RIGHT([CUSTOMER NAME], 3)
--ORDER BY 1

--7 REPLACE ALL SPACES IN CUSTOMER NAMES WITH UNDERSCORES AND CALCULATE THE TOTAL QUANTITY
--OF ITEMS ORDERED BY EACH MODIFIED NAME.
--SELECT REPLACE([CUSTOMER NAME], ' ', '_') AS UPDATED_NAME, SUM(QUANTITY) AS TOTAL_QUANTITY
--FROM TDI_SALES
--GROUP BY [CUSTOMER NAME]
--ORDER BY 1

--8 FIND THE LENGTH OF EACH CUSTOMER NAME AND DISPLAY THE RESULT, ORDERING BY THE LENGTH
--SELECT LEN([CUSTOMER NAME]) AS NAME_LENGTH
--FROM TDI_SALES
--ORDER BY NAME_LENGTH

--9 REMOVE LEADING AN TRAILING SPACES FROM CUSTOMER NAMES(IF ANY) AND 
-- DISPLAY THE RESULT, ORDERING BY THE TRIMMED NAMES
-- SELECT TRIM([CUSTOMER NAME]) AS TRIMMED_NAME_1
--FROM TDI_SALES

--10 REMOVE LEADING SPACES FROM THE CUSTOMER NAMES AND DISPLAY THE RESULT WHERE THE CUSTOMER NAME STARTS 
--WITH 'A' ORDERING BY THE TRIMMED NAMES.
--SELECT LTRIM([CUSTOMER NAME]) AS TRIMMED_NAME_2
--FROM TDI_SALES
--WHERE [CUSTOMER NAME] LIKE 'A%'
--GROUP BY [CUSTOMER NAME]
--ORDER BY TRIMMED_NAME_2

--11 REMOVE TRAILING SPACES FROM CUSTOMER NAMES AND DISPLAY THE RESULT WHERE THE 
--LENGTH OF THE CUSTOMER NAME IS GREATER THAN 10 ORDERING BY THE TRIMMED NAMES.
--SELECT LTRIM([CUSTOMER NAME]) AS TRIMMED_NAME, LEN([CUSTOMER NAME]) AS NAME_LENGTH
--FROM TDI_SALES 
--WHERE LEN([CUSTOMER NAME]) > 10
--GROUP BY [CUSTOMER NAME]
--ORDER BY TRIMMED_NAME

--12 DISPLAY CUSTOMER NAMES, REPLACING NULL VALUES WITH 'UNKNOWN' AND
--ORDER BY THE COALESCED CUSTOMER NAMES.
--SELECT [CUSTOMER ID], [CITY], [REGION], COALESCE([CUSTOMER NAME], 'UNKNOWN') AS COALESCED_NAME
--FROM TDI_SALES
--GROUP BY [CUSTOMER ID], [CITY], [REGION], [CUSTOMER NAME]
--ORDER BY 4
